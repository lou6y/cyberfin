package tn.esprit.spring.controller;

import java.util.List;

import org.junit.jupiter.api.Test;

import tn.esprit.spring.DAO.entities.Claim;

public class ClaimRestControllerTest {
/*	Claim claim=new Claim();
	
	@Test
	public List<Claim> testgetClaims() {
		Assert.assertNotNull(List<Claim>listClaims,claim.getClaims());
		//List<Claim> listClaims = claimService.retrieveAllClaims();
		return listClaims ;
		} */
}
